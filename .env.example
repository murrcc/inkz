# Anthropic API key — used by /api/whisper Cloudflare Function
# Set in Cloudflare Pages dashboard: Settings → Environment variables → Production
# For local dev, copy this file to .dev.vars (NOT .env)
ANTHROPIC_API_KEY=sk-ant-...
